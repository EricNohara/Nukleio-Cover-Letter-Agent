{
  "name": "nukleio-ai-agents",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "typecheck": "npm run build"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
